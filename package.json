{
  "name": "@scrapeapi/mcp",
  "version": "1.0.0",
  "description": "MCP server for ScrapeAPI — web scraping tools for Claude and LLM agents",
  "main": "dist/mcp-server.js",
  "bin": {
    "scrapeapi-mcp": "./dist/mcp-server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node mcp-server.ts",
    "start": "node dist/mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0"
  },
  "keywords": ["mcp", "scraping", "web-scraper", "llm", "claude", "anthropic"],
  "license": "MIT",
  "engines": { "node": ">=18" }
}
